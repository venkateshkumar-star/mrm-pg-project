apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-mrmpg-user
  labels:
    app: mrmpg-user
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: mrmpg-user
  template:
    metadata:
      labels:
        app: mrmpg-user
    spec:
      containers:
        - name: mrmpg-user
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
