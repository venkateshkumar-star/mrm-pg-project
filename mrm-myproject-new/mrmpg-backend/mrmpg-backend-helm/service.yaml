apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mrmpg-backend-service
  labels:
    app: mrmpg-backend
spec:
  type: {{ .Values.service.type }}
  selector:
    app: mrmpg-backend
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
