apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mrmpg-admin-service
  labels:
    app: mrmpg-admin
spec:
  type: {{ .Values.service.type }}
  selector:
    app: mrmpg-admin
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
