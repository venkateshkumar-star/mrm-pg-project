apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mrmpg-user-service
  labels:
    app: mrmpg-user
spec:
  type: {{ .Values.service.type }}
  selector:
    app: mrmpg-user
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
