apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-mrmpg-member
  labels:
    app: mrmpg-member
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: mrmpg-member
  template:
    metadata:
      labels:
        app: mrmpg-member
    spec:
      containers:
        - name: mrmpg-member
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
